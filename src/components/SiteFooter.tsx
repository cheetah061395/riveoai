/**
 * Footer. Right-hand content is unspecified in the handoff — left as a marked
 * placeholder rather than invented.
 */
export function SiteFooter() {
  return (
    <footer className="flex justify-between border-t border-ink/10 px-12 py-7 text-[13px] text-label">
      <span>Riveo</span>
      <span>[Footer content — not yet specified]</span>
    </footer>
  );
}

export default SiteFooter;
